# Taste (Continuously Learned by [CommandCode][cmd])
- Values authenticity — explicitly prohibits fabricated content (fake statistics, testimonials, client logos, company names, or achievements). Confidence: 0.9
- Prefers extremely detailed, numbered, hierarchical specifications covering every section, behavior, and constraint upfront before implementation begins. Will often write these specifications themselves rather than expecting the assistant to infer requirements. Confidence: 0.95
- Accessibility-conscious — requires semantic HTML, proper heading hierarchy, keyboard navigation, focus states, and accessible color contrast as a first-class requirement, not an afterthought. Confidence: 0.8
- Performance-conscious — expects optimized images, lazy loading, minimal JavaScript, fast page loads, and never leaving the user with a blank screen. Confidence: 0.8
- Prefers production-quality code conventions: TypeScript strict mode, reusable components, clean folder separation (app/components/data/lib/types), proper error handling, and proper loading states for all async actions. Confidence: 0.7
- Prefers one consistent design system (dark theme, single accent color, subtle gradients, generous spacing) rather than excessive colors or effects. Confidence: 0.7
- Prefers centralized CSS-variable-based theming for global UI state (accent colors, surface/background colors, design tokens) — all components should reference CSS variables from a single source of truth, not hardcoded values. This includes both accent (primary-*) and surface (dark-*) color scales, with mode switching (dark/light) handled by toggling CSS variable values at the document level via a `data-color-mode` attribute. Explicitly rejects manual per-component color management ("Do not manually change colors separately in every component"). Confidence: 0.85
- Prefers Context API / centralized React context over prop drilling for managing global UI state (e.g., theme, accent color). State logic should live in a single provider with a custom hook, never duplicated across components. Confidence: 0.8
- Prefers localStorage for persisting user preferences (e.g., selected accent theme) across sessions, with a fallback/default on first visit. Expects a save-on-change and restore-on-mount pattern. Confidence: 0.7
- Prefers predefined preset options (e.g., color swatches) over open-ended free-form pickers for user-facing customization controls — "Do not use a large intrusive color picker by default." The UI should remain minimal and professional. Confidence: 0.7
- Expects the website to support both dark and light color modes with a toggle button to switch between them. The toggle should be integrated into the existing theme/settings panel alongside the accent color picker, with the mode preference persisted in localStorage. Mode switching should be handled via CSS variable inversion (a `data-color-mode` attribute on `<html>` that swaps surface color scale values) so all existing `bg-dark-*` and `text-dark-*` classes adapt automatically without per-component changes. Confidence: 0.2
- When expanding a customization color palette, always preserve the original/default color in the expanded set — the original should remain available, not replaced or removed when new options are added. Said: "also incoude the origanal color that was first and also others." Confidence: 0.7
- Prefers a comprehensive, full-spectrum set of accent color options (e.g., 16 colors covering the complete Tailwind palette) over a small curated subset when offering theme customization. Wants breadth and variety so users have maximum choice. Said: "also add some colors." Confidence: 0.6
- Prefers a side-panel/drawer UI (slide-in from the right with backdrop blur overlay, spring animation) over a centered popover/modal for settings/configuration panels — explicitly referenced "ChatGPT UI" as the desired design pattern. Settings panels should be spacious, organized as vertical lists or clean grids, and feel premium with spring transitions and backdrop blur. Confidence: 0.75
- Prefers theme/accent color changes to be instant and non-disruptive: no page reload, no scroll position reset, no animation interruption, no flashing. Expects smooth CSS transitions on color/border/background properties when the theme changes. Confidence: 0.75
- Values a consistent global loading system (preloader, button spinners, skeleton states, form submission states) applied uniformly across the entire application, not ad-hoc per component. Explicitly expects the same loader to appear on navigation actions (e.g. navbar button clicks) as everywhere else. Confidence: 0.8
- Prefers a single, identical loader component reused for ALL loading scenarios (initial page load, route transitions, button loading, form submissions) rather than different loader types per context. Confidence: 0.6
- Prefers subtle, professional animations that respect `prefers-reduced-motion` — never excessive, never slowing down the website. Will explicitly revert (ask to undo) if animations feel too elaborate (e.g., gradient orbs, scroll progress bars, stagger reveals, timeline line animations, skill progress bars). Confidence: 0.8
- Prefers CSS-based modern web animations (scroll progress bars, gradient orbs, stagger reveals, scroll-triggered progress bars) over Three.js/WebGL 3D effects. Rejects heavy 3D libraries for visual polish in favor of lightweight, performant CSS animations. Explicitly tells the assistant to remove 3D scenes and animations entirely when they are added without prior approval. However, when the user explicitly requests 3D/Three.js visualizations themselves, expects a sophisticated, interactive implementation with mouse parallax, performance optimization (GPU, prefers-reduced-motion, mobile particle reduction), responsive adaptation across breakpoints, and a configurable, reusable component API. The visual should feel premium, futuristic, and professional — never a basic 2D atom icon or generic spinning globe. Confidence: 0.85
- Expects mobile-first responsive design with responsive typography, spacing, navigation, and touch-friendly targets across desktop, laptop, tablet, and mobile. Confidence: 0.7
- Expects decorative visual effects and animations (e.g., orbital systems, 3D elements, background animations) to be visible on mobile — not hidden with `hidden` or display:none on small screens. If shown on mobile, they should be appropriately resized/scaled down to fit the smaller viewport rather than removed entirely. Confidence: 0.6
- Expects mobile navigation overlay to lock body scroll when open (via `document.body.style.overflow = "hidden"`), preventing the page background from scrolling behind the menu — matching the standard UX pattern used on professional websites. Confidence: 0.8
- Expects comprehensive SEO metadata (Open Graph, Twitter cards, semantic HTML, proper heading hierarchy) as a built-in requirement, not an optional add-on. Confidence: 0.7
- Prefers pixel-perfect/exact replication of provided visual references (images, design examples) — reproduce them faithfully, not as creative interpretations or approximations. Confidence: 0.6
- Prefers a specific visual aesthetic for loading indicators: glowing tick/line marks radiating outward from center (like a radar or compass), rotating around the text, rather than solid conic-gradient rings or filled spinners. Confidence: 0.7
- Prefers a premium, multi-element full-screen loading screen assembled from separate reusable components (radial loader + loading text with animated dots + progress bar) that all coordinate together, rather than a single simple spinner. Confidence: 0.9
- Prefers a deliberate, unhurried initial loading screen duration (~5–6 seconds) for a premium/professional feel — the loader should have enough time to display its full animation cycle (radial traveling light, sequential dots, progress bar filling) rather than being too quick to register. Confidence: 0.6
- Prefers smooth progress animations driven by requestAnimationFrame with ease-out curves, never instant jumps — progress should feel natural and connected to actual loading state when possible, with graceful completion (smooth to 100% → hold briefly → exit transition) if the page loads early. Confidence: 0.9
- Prefers animated loading dots that appear one by one in sequence and loop continuously (blank → . → .. → ... → repeat), not static or all-visible-at-once dots. Confidence: 0.8
- Prefers explicit exit transitions for overlays/loading screens (fade-out with optional subtle scale, using AnimatePresence), never sudden disappearance. Confidence: 0.8
- Specifies exact file paths and component names when requesting new components (e.g., "components/ui/RadialLoader.tsx", "components/ui/LoadingDots.tsx", "components/ui/LoadingProgress.tsx", "components/ui/InitialLoader.tsx") — expects generated code to precisely match the declared path structure. Confidence: 0.9
- Prefers a specific loader animation cycle: segments should appear one by one sequentially around the circle (filling phase), then the completed entire ring should rotate 360° once with a smooth ease-out deceleration (rotation phase), then briefly reset and restart — a continuous "fill → rotate → reset → repeat" pattern. This loop should persist for the entire duration the loader is visible ("it should revolve until line fills"), not just complete a single cycle and stop. This is the professional website loader pattern. Segments must stay lit (not fade individually) and only disappear/reset after the full revolution and rotation are complete. Not a one-shot deceleration or simultaneous appearance. Confidence: 0.7
- Prefers a deliberate, slower-paced loader animation speed for the fill→rotate cycle — loader cycles should be unhurried so they visibly span the full loading duration (e.g., ~1.2s for medium, ~1.8s for large), allowing the complete animation sequence (segments filling, ring rotating) to be registered by the user. Will explicitly ask to slow down fast cycles. Confidence: 0.4
- Prefers a true revolving/traveling-light animation for radial loaders: segments must remain fixed in their positions around the circle — the container must NOT rotate as one static object. Only the light intensity/highlight should travel sequentially from segment to segment (Segment 1 → Segment 2 → Segment 3 → ...). Each segment uses a staggered CSS animation delay so the bright glowing segment continuously moves around the circle, with smooth brightness transitions (dim → bright with glow → fading → dim). The result should visually show the light revolving around the loader, not the whole loader spinning. Confidence: 0.99
- Prefers navigation loaders to be transparent/non-blocking overlays: the loader should appear centered on screen without a solid background overlay, so the page content remains visible behind it during navigation. Do not use a black or opaque background that obscures the page. Confidence: 0.6
- Prefers a "pending changes" workflow — wants to be able to revert/undo changes after seeing the result, and will explicitly signal approval (e.g. "yes") before changes are considered final. Do not treat changes as committed until user confirms. Confidence: 0.6
- Prefers to handle destructive/npm/package-management shell commands themselves — will explicitly stop the assistant from running commands and take over in their own terminal (e.g. PowerShell). Do not run commands that delete node_modules, package-lock, or perform clean installs without asking first. Confidence: 0.5
- Prefers a zero-friction local dev setup where `npm run dev` is the sole command needed to start working — no separate build steps, no configuration commands, no multiple terminals required. The dev server must NOT trigger any implicit dependency downloading or installation during `npm run dev`; all dependencies must be fully pre-installed so that the command purely starts the dev server with no side effects. Expects the project to "just work" with that single command. Confidence: 0.7
- Expects defensive/resilient data handling — if a data field is removed from a data file (e.g. `period` on an experience entry), the code should gracefully handle it without TypeScript errors and without rendering empty UI elements (e.g., empty badge spans). Prefers optional types and conditional rendering over required fields that would break when data is simplified. Confidence: 0.5
- Prefers preserving static/SSG rendering over opting pages into dynamic rendering — when a component uses browser-only libraries (e.g. Three.js), prefers component-level SSR guards (useEffect mount check with placeholder) rather than exporting `force-dynamic` on the page. Explicitly rejects `force-dynamic` as a solution. Confidence: 0.9
- Expects the assistant to stay strictly within the scope of what was explicitly requested — proactively adding unrequested features (e.g., dark/light mode toggle) will be immediately reverted. Do not implement features, UI components, or enhancements beyond the specific task the user has asked for, even if they seem like natural extensions or align with previously stated preferences. The user prefers to drive the feature roadmap themselves. Confidence: 0.6
- Prefers self-contained, all-in-Next.js architecture with zero external dependencies for content management — no separate backend, no database, no external CMS (e.g. Strapi, Sanity, WordPress). Wants a single admin panel built fully within Next.js using file-based JSON storage and API routes. Explicitly requests the admin solution be "completely in next.js only." Confidence: 0.7
- Prefers polite, user-friendly UX copy text — explicitly asked for "Please Wait" instead of "Loading" in the global preloader, indicating a taste for considerate, descriptive phrasing over generic or technical labels. Confidence: 0.1
- Preloader animations must run for their full designated duration regardless of actual page load speed — the preloader should NOT be bypassable by early content readiness. No `__loaderReady` / early-finish mechanism should exist. The loader is a timed showcase that should play its complete animation cycle (fill → rotate → dots → progress) before revealing the website, not snap to 100% when content renders early. Confidence: 0.8
- Prefers surgically precise, scoped reverts/changes — when asking to revert a specific component's style/behavior (e.g., RadialLoader), explicitly expects ONLY that component to be modified, with zero collateral changes to related/tangential components (e.g., LoadingDots, LoadingProgress, InitialLoader). Will explicitly name which parts to exclude from changes ("not the loading or ling"). No collateral damage. Confidence: 0.9
- Follows up to audit implementation completeness — after providing a detailed specification, will proactively check whether every item was fully implemented ("check the prompt i give you at the last is completed"). Expects thorough, complete implementation of all specified requirements, not partial delivery. Confidence: 0.4
- Expects visual backward compatibility when introducing theming/customization features — the default/original visual appearance (colors, shadows, hover states, all design tokens) must remain pixel-identical to what existed before the feature was added. Explicitly objects if the site looks different after theming is introduced, even if the new defaults are semantically equivalent hex values. The pre-existing design must be faithfully preserved so returning visitors perceive zero visual change. Said: "you have also chnged the website color keep it orignal." Confidence: 0.8
